{
  "name": "aqi-nowcast-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that fires the ingest workflow on a cron GitHub does not throttle.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  }
}
